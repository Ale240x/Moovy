<!DOCTYPE html>
<html lang="it">
<head>
    <%- include('../partials/head.ejs') %>
    </style>
    <title>Moovy</title>
</head>
<body>
    <%- include('../partials/navbarClie.ejs') %>
   
    <div class="container mx-auto" style="height: 512px;" >
      <br>
    
    <% if(!prenotazioni|| prenotazioni.length == 0) {  %>  
    
      <div class="text-center" style="height: 400px;"><h2><br><br><br><br><br>Nessuna prenotazione modificabile!</h1></div>
    
    <% }  else { %>
    
      <div class="container">
        <h2>Prenotazioni effettuate</h2>
        <p>Ecco la lista di tutte le prenotazioni effettuate, seleziona quella che vuoi modificare</p>           
          
            <table class="table table-bordered table-bordered-success table-hover">
                <thead>
                  <tr>
                    <th>ID Prenotazione</th>
                    <th>Luogo ritiro</th>
                    <th>Stato</th>                     
                  </tr>
                </thead>
                <tbody>
                  <% prenotazioni.forEach( (prenotazione)=>{ %>
                  <tr>
                    <td><a href="/utente/cliente/AreaPersonaleCliente/ElencoPrenotazioni/<%= prenotazione.id_prenotazione %>"><%= prenotazione.id_prenotazione %></a></td>
                    <td><%= prenotazione.luogo_ritiro %></td>
                    <td><%= prenotazione.stato_prenotazione %></td>
                  </tr>
                  <% }) %>
                </tbody>
              </table>
    <% } %>   
        
    </div>
    </div>

<!--footer-->   
<%- include('../partials/footer.ejs') %>
</body>
</html>